#include <stdio.h>

#typedef char *set
private:
	set *A;
	set *B;
	set *C;
	set *D;
	set *E;
	set *F;
public:
	void union_set();
	void intersect_set();
	void sub_set();
	void halt();
*	void readset(char *c, set *c);

